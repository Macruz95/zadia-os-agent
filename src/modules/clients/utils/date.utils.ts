import { formatDate, formatDateTime, calculateAge } from '@/lib/date-utils';

/**
 * Client-specific date utilities
 * Re-exports common date functions for client module
 */

// Re-export common date utilities
export { formatDate, formatDateTime, calculateAge };

// Client-specific date formatting if needed in the future
// Add any client-specific date logic here