import { calculateAge } from '@/lib/date-utils';

/**
 * Age calculation utilities for client module
 * Re-exports age calculation from main date utilities
 */

// Re-export age calculation
export { calculateAge };

// Client-specific age utilities if needed in the future
// Add any client-specific age logic here